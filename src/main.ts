import { createApp } from 'vue'
import './style.css'
// import { Keyboard } from '@capacitor/keyboard'
import App from './App.vue'

createApp(App).mount('#app')

// Keyboard.setScrollBehavior({ behavior: 'keyboard' }) // авто-скролл к активному полю